{
  "training": {
    "num_iterations": 10,
    "games_per_iteration": 10,
    "sims_per_move": 20,
    "learning_rate": 0.001,
    "checkpoint_dir": "checkpoints"
  },
  "evaluation": {
    "eval_games": 50,
    "sims_per_eval": 40,
    "checkpoint_path": "checkpoints/model_final.bin"
  },
  "mcts": {
    "c_puct": 1.4
  }
}
